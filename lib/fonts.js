import { Inter } from "next/font/google"

export const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
  display: "swap",
})

// Use Inter for all font needs to avoid Google Fonts loading issues
export const syne = inter
export const satoshi = inter
